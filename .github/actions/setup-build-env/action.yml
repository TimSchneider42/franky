name: Setup build environment
description: Install system packages, cache build deps, and run install-dependencies

inputs:
  libfranka-version:
    required: true
  manylinux-version:
    required: true

runs:
  using: composite
  steps:
    - name: Install yum dependencies
      run: yum -y install zip unzip boost-devel fmt-devel openssl-devel mysql-devel postgresql-devel
      shell: bash

    - name: Install other dependencies
      run: LIBFRANKA_VERSION=${{ inputs.libfranka-version }} ./docker/common/install-dependencies
      shell: bash

    - name: Refresh linker cache
      run: ldconfig
      shell: bash
